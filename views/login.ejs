<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body { font-family: Arial, sans-serif; padding: 24px; background: #f5f5f5; }
    .card { max-width: 420px; margin: 40px auto; background: white; padding: 18px; border-radius: 10px; box-shadow: 0 8px 20px rgba(0,0,0,0.12); }
    .row { display:flex; flex-direction:column; gap:6px; margin-bottom:12px; }
    input { padding:10px; border:1px solid #ccc; border-radius:8px; font-size:14px; }
    button { padding:10px 14px; border-radius:8px; border:0; background:#2196F3; color:white; font-weight:700; cursor:pointer; }
    .err { color:#F44336; margin: 8px 0; }
    .mini { font-size:12px; color:#666; }
  </style>
</head>
<body>
  <div class="card">
    <h2 style="margin:0 0 12px 0;">Login</h2>
    <% if (error) { %><div class="err"><%= error %></div><% } %>
    <form method="POST" action="/login">
      <div class="row">
        <label>Username</label>
        <input name="username" autocomplete="username" required />
      </div>
      <div class="row">
        <label>Password</label>
        <input name="password" type="password" autocomplete="current-password" required />
      </div>
      <button type="submit">Sign in</button>
    </form>
    <p class="mini" style="margin-top:12px;">Ask the admin for credentials.</p>
  </div>
</body>
</html>


