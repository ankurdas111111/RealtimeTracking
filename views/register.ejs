<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#2563eb">
  <title>Register - Real-time Tracker</title>
  <link rel="icon" href="/favicon.svg" type="image/svg+xml">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(1200px 600px at 20% 0%, rgba(37,99,235,0.12), transparent 55%),
                  radial-gradient(900px 600px at 90% 10%, rgba(156,39,176,0.10), transparent 50%),
                  #f6f8fb;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
      padding: 16px;
    }
    .auth-card {
      width: 100%;
      max-width: 420px;
      background: rgba(255,255,255,0.92);
      backdrop-filter: blur(12px);
      padding: 28px 24px;
      border-radius: 18px;
      border: 1px solid rgba(15, 23, 42, 0.10);
      box-shadow: 0 20px 50px rgba(2, 6, 23, 0.14);
    }
    .auth-card h2 {
      margin: 0 0 4px 0;
      font-size: 22px;
      font-weight: 800;
      color: #0f172a;
    }
    .auth-card .subtitle {
      font-size: 13px;
      color: #64748b;
      margin-bottom: 18px;
    }
    .field {
      display: flex;
      flex-direction: column;
      gap: 5px;
      margin-bottom: 14px;
    }
    .field label {
      font-size: 13px;
      font-weight: 600;
      color: #334155;
    }
    .field input {
      padding: 11px 14px;
      border: 1px solid rgba(15, 23, 42, 0.14);
      border-radius: 10px;
      font-size: 14px;
      background: rgba(255,255,255,0.9);
      color: #0f172a;
      outline: none;
      transition: border-color 0.15s, box-shadow 0.15s;
    }
    .field input:focus {
      border-color: rgba(37, 99, 235, 0.4);
      box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.10);
    }
    .field .hint {
      font-size: 11px;
      color: #94a3b8;
    }
    .err {
      background: rgba(239,68,68,0.08);
      color: #dc2626;
      padding: 8px 12px;
      border-radius: 8px;
      font-size: 13px;
      margin-bottom: 12px;
      border: 1px solid rgba(239,68,68,0.15);
    }
    .auth-btn {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 12px;
      background: linear-gradient(180deg, rgba(37,99,235,0.98), rgba(29,78,216,0.98));
      color: #fff;
      font-size: 15px;
      font-weight: 800;
      cursor: pointer;
      box-shadow: 0 14px 28px rgba(37,99,235,0.22);
      transition: transform 0.08s, filter 0.15s;
    }
    .auth-btn:hover { filter: brightness(1.05); }
    .auth-btn:active { transform: translateY(1px); }
    .auth-link {
      margin-top: 14px;
      text-align: center;
      font-size: 13px;
      color: #64748b;
    }
    .auth-link a {
      color: #2563eb;
      text-decoration: none;
      font-weight: 600;
    }
    .auth-link a:hover { text-decoration: underline; }
    .contact-toggle {
      display: flex;
      gap: 0;
      border: 1px solid rgba(15,23,42,0.14);
      border-radius: 10px;
      overflow: hidden;
    }
    .toggle-btn {
      flex: 1;
      padding: 10px 0;
      border: none;
      background: rgba(255,255,255,0.9);
      color: #64748b;
      font-size: 13px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.15s, color 0.15s;
    }
    .toggle-btn.active {
      background: linear-gradient(180deg, rgba(37,99,235,0.98), rgba(29,78,216,0.98));
      color: #fff;
    }
    .toggle-btn:not(.active):hover {
      background: rgba(37,99,235,0.06);
    }
  </style>
</head>
<body>
  <noscript><div style="padding:2rem;text-align:center;"><h2>JavaScript Required</h2><p>Please enable JavaScript.</p></div></noscript>
  <div class="auth-card">
    <h2>Create account</h2>
    <p class="subtitle">Sign up to start tracking and sharing</p>
    <% if (error) { %><div class="err"><%= error %></div><% } %>
    <form method="POST" action="/register">
      <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
      <input type="hidden" name="contact_type" id="contactType" value="email">
      <div class="field">
        <label for="username">Username</label>
        <input id="username" name="username" autocomplete="username" required autofocus />
        <span class="hint">3-20 chars, lowercase letters, numbers, underscores</span>
      </div>
      <div class="field">
        <label for="password">Password</label>
        <input id="password" name="password" type="password" autocomplete="new-password" required />
        <span class="hint">Minimum 8 characters</span>
      </div>
      <div class="field">
        <label for="confirm">Confirm password</label>
        <input id="confirm" name="confirm" type="password" autocomplete="new-password" required />
      </div>
      <div class="field">
        <label style="font-size:13px; font-weight:600; color:#334155; margin-bottom:4px;">Contact method</label>
        <div class="contact-toggle">
          <button type="button" class="toggle-btn active" id="toggleEmail" aria-label="Use email">Email</button>
          <button type="button" class="toggle-btn" id="toggleMobile" aria-label="Use mobile number">Mobile</button>
        </div>
      </div>
      <div class="field" id="contactField">
        <label for="contact_value" id="contactLabel">Email address</label>
        <input id="contact_value" name="contact_value" type="email" autocomplete="email" placeholder="you@example.com" required />
        <span class="hint" id="contactHint">Used by others to find and add you as a contact</span>
      </div>
      <button class="auth-btn" type="submit" aria-label="Create account">Create account</button>
    </form>
    <script>
    (function() {
      var emailBtn = document.getElementById('toggleEmail');
      var mobileBtn = document.getElementById('toggleMobile');
      var contactType = document.getElementById('contactType');
      var contactInput = document.getElementById('contact_value');
      var contactLabel = document.getElementById('contactLabel');
      function setEmail() {
        emailBtn.classList.add('active'); mobileBtn.classList.remove('active');
        contactType.value = 'email';
        contactInput.type = 'email'; contactInput.autocomplete = 'email';
        contactInput.placeholder = 'you@example.com';
        contactLabel.textContent = 'Email address';
      }
      function setMobile() {
        mobileBtn.classList.add('active'); emailBtn.classList.remove('active');
        contactType.value = 'mobile';
        contactInput.type = 'tel'; contactInput.autocomplete = 'tel';
        contactInput.placeholder = '+1234567890';
        contactLabel.textContent = 'Mobile number';
      }
      emailBtn.addEventListener('click', setEmail);
      mobileBtn.addEventListener('click', setMobile);
    })();
    </script>
    <p class="auth-link">Already have an account? <a href="/login">Sign in</a></p>
  </div>
</body>
</html>
